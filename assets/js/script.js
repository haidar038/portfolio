function mode(x) {
    var element = document.body;
    element.classList.toggle("dark-mode");
    x.classList.toggle("bi-toggle-on");
}

function nav(mode) {
    var navbar = document.getElementsByClassName("dark-mode-navbar");
}